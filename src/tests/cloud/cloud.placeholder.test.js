/**
 * Dummy cloud-suite example. Tests under src/tests/cloud/ are skipped by default
 * (see package.json test scripts: testPathIgnorePatterns). Run everything with npm run test:all.
 */
describe("cloud (placeholder)", () => {
  it("dummy assertion so the cloud folder has a trivial always-pass test", () => {
    expect(true).toBe(true);
  });
});
