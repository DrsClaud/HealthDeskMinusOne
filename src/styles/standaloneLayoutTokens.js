/**
 * Shared style tokens for standalone / full-bleed dashboard pages that render
 * content inside MUI Card or Paper components.
 */

/** Applies to `Card variant="outlined"` content cards throughout standalone pages. */
export const contentCardSx = {
  borderRadius: 2,
};
